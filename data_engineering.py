# 🔥 Data Engineering Focused Function Problems
# 16. Write a function to read a CSV file and return the number of rows.

import my_string as rl
userInput = input('Enter elements of list separated by space , so that i will reverse it for you: ').split()
numList = [int(num) for num in userInput]
a =rl.reverseList(numList)